import CustomerLayout from '../layout/_role-based/CustomerLayout';
import { Outlet } from 'react-router-dom';

export default function Customer() {
    return (
        <CustomerLayout pageTitle="Customer Dashboard">
            
        </CustomerLayout>
    );
}